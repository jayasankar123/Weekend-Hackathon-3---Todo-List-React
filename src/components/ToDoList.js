import React from "react";

export default function ToDoList({ content, del }) {
  function handleDelete(delContent) {}
  function handleEdit(editContent, indexel) {}
  return <></>;
}
//onClick={del(content)}
